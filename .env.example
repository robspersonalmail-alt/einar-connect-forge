# White-Label Author Template Configuration

# Default tenant to load when no domain mapping is found
# This should match a JSON file in src/brands/
VITE_DEFAULT_TENANT=default

# Optional: Add analytics or other environment variables here
# VITE_GA_TRACKING_ID=
# VITE_PLAUSIBLE_DOMAIN=